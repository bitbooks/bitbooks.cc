# Rakefile
# Defines helpful rake tasks we can run from command line.
# See: http://rake.rubyforge.org/doc/rakefile_rdoc.html

# I added these lines at some point but I don't think they make sense, so I am
# commenting them out to see if anything breaks. If not, I'll remove them.
#
#require 'sidekiq'
#require 'sidekiq/web'
#require 'redis'
#require './app'

require 'sinatra/activerecord/rake' # Defines migration tasks.
